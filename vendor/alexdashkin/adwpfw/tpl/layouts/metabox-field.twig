<div class="adwpfw-field">

    {% block label %}

        {% if field.label %}
            <div class="adwpfw-label">
                <label for="{{ prefix }}-{{ field.form }}-field-{{ field.id }}">{{ field.label }}</label>
            </div>
        {% endif %}

    {% endblock %}

    <div class="adwpfw-control">

        {% block control %}
        {% endblock %}

        {% block desc %}

            {% if field.desc %}
                <div class="adwpfw-desc">{{ field.desc }}</div>
            {% endif %}

        {% endblock %}

    </div>

</div>
