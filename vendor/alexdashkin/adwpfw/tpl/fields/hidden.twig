{% extends 'layouts/' ~ field.layout ~ '.twig' %}

{% block control %}

        <input type="hidden"
               id="{{ prefix }}-{{ field.form }}-field-{{ field.id }}"
               name="{{ prefix }}[{{ field.form }}][{{ field.id }}]"
               value="{{ field.value }}">

{% endblock %}
