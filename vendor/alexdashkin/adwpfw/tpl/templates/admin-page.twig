<div class="wrap adwpfw-admin-page adwpfw {{ prefix }}">

    <header><h1>{{ title }}</h1></header>

    <nav class="adwpfw-tabs-header nav-tab-wrapper">

        {% for tab in tabs %}
            <span class="nav-tab">{{ tab.title }}</span>
        {% endfor %}

    </nav>

    {% for tab in tabs %}

        {% include 'templates/admin-page-tab.twig' %}

    {% endfor %}

</div>